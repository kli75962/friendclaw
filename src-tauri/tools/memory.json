{
  "type": "function",
  "function": {
    "name": "memory",
    "description": "Read and maintain long-term memory stored in /.memory files.\n\nMemory files:\n- /memories/core.md       — Core facts about the user (language, name, preferences). Injected into every prompt. Keep it short.\n- /memories/notes.md      — Detailed knowledge: UI navigation paths, timestamped observations, longer notes.\n- /memories/conversations.jsonl — Past conversation log for recall search.\n\nRules:\n- If the user's request might depend on preferences, history, or past navigation, search first.\n- If the user states a preference or personal fact, save it to core.md.\n- If you discover a reliable UI path (e.g. how to reach a setting), save it to notes.md.\n- Keep memory operations invisible in user-facing replies.\n- Keep core.md short and focused. Use notes.md for detail.\n- Format navigation paths as: \"To [action]: App > Screen > Element\"",
    "parameters": {
      "type": "object",
      "required": ["command"],
      "properties": {
        "command": {
          "type": "string",
          "enum": ["view", "create", "update", "search"],
          "description": "Memory action: view to read a file, create to write a new file, update to change an existing file, search to find relevant lines across files."
        },
        "path": {
          "type": "string",
          "description": "Memory file path. Required for view, create, update. One of: /memories/core.md, /memories/notes.md, /memories/conversations.jsonl"
        },
        "content": {
          "type": "string",
          "description": "Text to write. Required for create and update commands."
        },
        "mode": {
          "type": "string",
          "enum": ["append", "overwrite"],
          "description": "Write mode for update: 'append' adds to the end of existing content, 'overwrite' replaces the entire file. Defaults to overwrite."
        },
        "query": {
          "type": "string",
          "description": "Search keywords for the search command. Use 1-4 focused words."
        }
      }
    }
  }
}
