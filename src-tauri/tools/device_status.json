{
  "type": "function",
  "function": {
    "name": "device_status",
    "description": "Check if a device is currently online and reachable. Useful before attempting to control another device remotely. Returns 'online' or 'offline' status.",
    "parameters": {
      "type": "object",
      "properties": {
        "device_id": {
          "type": "string",
          "description": "The device ID to check (e.g. the UUID of a paired device, or empty for 'this device'). If empty, reports this device as always online."
        }
      },
      "required": []
    }
  }
}
